{
  "name": "jsblocks-seed",
  "description": "Starting template for a jsblocks project",
  "version": "0.0.1",
  "repository": "https://github.com/astoilkov/jsblocks-seed",
  "licence": "MIT",
  "private": true,
  "scripts": {
    "postinstall": "cd app && bower install",
    "start": "cd app && http-server -a localhost -p 8000 -c-1 -o",
    "node": "opener http://localhost:8000 && node app.js"
  },
  "devDependencies": {
    "blocks": "0.x.x",
    "bower": "^1.4.1",
    "http-server": "^0.8.0",
    "opener": "^1.4.1"
  }
}
